package view;
/*
Vue secondaire, perspective secondaire, bref c'est secondaire.
 */
import model.ImageModel;
import model.Perspective;

public class SecondaryView extends AbstractView {

    public SecondaryView(ImageModel model, Perspective p) {
        super(model, p);
    }
}
